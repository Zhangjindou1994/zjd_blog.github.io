# 行锁问题排查介绍

Oceanbase的行锁问题在每个版本中，略有差异，本文以OB 4.3.5.1为例，展开行锁问题的研究。

## 相关视图

### gv$ob_locks

视图 `GV$OB_LOCKS` 中记录了当前用户中各表持有锁或请求锁的情况。该视图中主要展示以下 3 种类型的锁：

- 表锁（TM）：表或分区（Tablet）上的锁。通常 insert / update / delete 等 DML 操作会产生分区级表锁，而 DDL 操作会产生表级表锁。
- 行锁（TR）：写行时上的锁。
- 事务锁（TX）：最常见的锁，通常可以理解为在事务内做过写操作即会产生事务锁。



## 测试案例

- 创建测试数据表

```
：create table zjd.test_lock(id number,name varchar2(20),constraint pk_id  primary key (id))


insert into zjd.test_lock values(1,'aaa');
insert into zjd.test_lock values(2,'bbb');
insert into zjd.test_lock values(3,'ccc');
insert into zjd.test_lock values(4,'ddd');


update zjd.test_lock set name='eee' where id=1;

```

- 连接业务租户，创建会话A，并更新第一条记录

```
update zjd.test_lock set name='eee' where id=1;
```

查看行锁情况

```
SELECT t.TRANS_ID, t.TYPE, t.ID1, t.ID2, t.ID3,t.LMODE, t.REQUEST, ROUND(t.CTIME / 1000000) CTIME_s, t.BLOCK 
  FROM SYS.gv$ob_locks t 
--   WHERE t.BLOCK = 1 
  ORDER BY t.TRANS_ID;
```

![image-20251028152334787](C:\Users\zhang\AppData\Roaming\Typora\typora-user-images\image-20251028152334787.png)

从结果上可以看到，当执行行记录更新之后，会在表上占有3把锁，一是TR锁，且是行级排他锁，二是TX事务锁，3是表级的共享排他锁(TM锁)。

- 连接业务租户，创建会话B，并更新会话A中同一条记录

  ```
  update zjd.test_lock set name='fff' where id=1;
  ```

  行锁结果截图如下:

![image-20251028153956252](C:\Users\zhang\AppData\Roaming\Typora\typora-user-images\image-20251028153956252.png)

可以看到，新的会话所i发起的事务id为 3598231075，申请的是行锁，block为1，表示被阻塞，ID1和ID2表示持有锁的trans_id和会话id